# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/

# Logs / temp
*.log
logs/
tmp/
.cache/

# Secrets / environment
.env
.env.*
!.env.example
*.pem
*.key
*.crt
*.p12
*.pfx
.fernet_key

# Fichiers temporaires SQLite
*.db-shm
*.db-wal

# Base SQLite locale : contient des données clients réelles (KYC), ne doit
# pas être versionnée ni poussée sur le dépôt distant.
/diaspora_onboarding.db

# Sensitive runtime data
# data/api_integrations.json est volontairement versionné (dépôt privé) pour
# retrouver la config Callbell en changeant de poste.
data/pre_onboarding_otps.json
data/*.db
data/*.sqlite
data/*.sqlite3
data/*.sqlite*

# Uploads
uploads/
app/uploads/
static/uploads/

# Backups / snapshots
*.bak
*.bak_*
*.bak*
*STABLE_*
*BROKEN_*
*UNSTABLE_*
*.STABLE_*
*.BROKEN_*
*.UNSTABLE_*

# Accidental shell files
/0
/=

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
.claude/

# Binaires locaux (tunnel)
cloudflared-windows-amd64.exe

# Build / archives
node_modules/
dist/
build/
*.zip
*.tar
*.tar.gz
*.rar
# Journaux de paiement Mastercard : contiennent des données carte/client réelles
# (nom, carte masquée, téléphone) — ne doivent pas être versionnés.
data/mastercard_payment_records.jsonl
data/mastercard_payment_events.jsonl
